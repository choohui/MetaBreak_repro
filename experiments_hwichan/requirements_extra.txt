# In addition to repro_mb/requirements.txt.
# scikit-learn powers the per-layer logistic-regression separability probe in
# analyze_representations.py. If it is absent, the code falls back to a
# dependency-free nearest-centroid probe and pure-NumPy ROC-AUC, so this is
# optional but recommended.
scikit-learn>=1.3,<2.0
