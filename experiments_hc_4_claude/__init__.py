"""experiments_hc_4_claude — MetaBreak semantic-mimicry defense study (Main.md).

Self-contained: depends only on repo-level ``src/*``, ``prompts/*`` and
``results/llama/replacement.json`` — never on other ``experiments_*`` folders.
"""
