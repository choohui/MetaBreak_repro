"""Numbered pipeline stages (00–07) + shared analysis helpers.

Each ``NN_*.py`` is one Main.md stage; ``run_all`` loads them by file path
(digit-prefixed names are not importable as modules). ``analysis_common`` holds
the helpers reused by the analysis stages (04–07).
"""
