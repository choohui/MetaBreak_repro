"""repro_mb stages — MetaBreak TM-1 (semantic mimicry) reproduction.

Each module in this package implements one stage of the attack and can be
invoked either via the top-level `run.py` orchestrator or standalone with
`python -m src.<stage> --model_type X --model <hf_path>`.
"""
